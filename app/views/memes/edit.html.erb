<h1>Editing Meme</h1>

<%= render 'form', meme: @meme %>

<h2>Commands</h2>
<%= render @meme.commands %>

<h2>Add a command:</h2>
<%= render 'commands/form' %>

<%= link_to 'Show', @meme %> |
<%= link_to 'Back', memes_path %>
